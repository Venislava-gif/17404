#include <stdio.h>
int main() {
    double a, b, c,x1,x2;
    printf("Enter: a, b and c: ");
    scanf( &a, &b, &c);
     d = b*b-4*a*c;
     if(d<0)
     {
         printf("No real root");
     }else(d>0)
     {
  
     }
     
